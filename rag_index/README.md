# rag_index

This folder is generated by running:

```bat
python build_document01_index.py
```

Expected generated files:

- `chunks01.json`
- `faiss01.index`

These files are not precomputed in this ZIP because they depend on the installed embedding model and local environment.
